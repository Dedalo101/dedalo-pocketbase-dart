name: dedalo_pocketbase
description: Dedalo101-maintained Dart SDK for PocketBase — independent fork for the Dedalo artist platform and Core stack.
repository: https://github.com/Dedalo101/dedalo-pocketbase-dart
homepage: https://github.com/Dedalo101/dedalo-pocketbase-dart

version: 0.24.0+dedalo1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.3.0
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.2.0
  json_serializable: ^6.0.0
  lints: ^5.0.0
  test: ^1.16.0